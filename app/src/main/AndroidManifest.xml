<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- INTERNET é necessário para osmdroid (download de tiles) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Estado da rede (útil para decidir se usar tiles em cache / network) -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- 📍 Localização (foreground e background) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- 👣 Reconhecimento de atividade / passos (runtime em Android 10+) -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- 🕒 Serviço em primeiro plano (para tracking contínuo) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- 💾 Ler / Escrever ficheiros (o teu pedido = leitura/escrita) -->
    <!--    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />-->
    <!--    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />-->

    <!-- 🔔 Notificações: necessário solicitar em runtime no Android 13+ (targetSdk >= 33) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 🔋 Wake lock: útil se usares foreground service para garantir CPU ativo durante tracking -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Volt_Project">
        <activity
            android:name=".DashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.Volt_Project.NoActionBar" />
        <activity
            android:name=".AddInfoActivity"
            android:exported="false" />
        <activity
            android:name=".RequestPermissionsActivity"
            android:exported="false" />
        <activity
            android:name=".RunningActivity"
            android:exported="false" />
        <activity
            android:name=".RecoverEmailActivity"
            android:exported="false" />
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="false" />
        <activity
            android:name=".HiActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>